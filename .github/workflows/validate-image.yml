name: Node.js Docker Build and Run

on:
  push:
  pull_request:

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Build frontend
        run: npm run --workspace=app build

      - name: Build and run Docker container
        run: |
          aws s3 cp s3://evmiguel-config/.env ./.env

          docker build -t tag-api-image ./api
          docker run -d -p 3000:3000 tag-api-image
          docker build -t git-tag-app ./app
          docker run -d -p 8080:80 git-tag-app

      - name: Install dependencies for Cypress
        run: npm install

      - name: Run tests
        run: |
          sleep 5
          npm run test

      - name: Stop docker container
        run: |
          docker stop $(docker ps -q)
